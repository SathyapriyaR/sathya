#include"stdio.h"
#include"string.h"
void main()
{
char str[50],a[50];
int i,k;
printf("\nEnter the string:");
scanf("%s",&str[i]);
printf("\nEnter the number:");
scanf("%d",&k);
for(i=0;i<k;i++)
{
a[i]=str[i];
}
printf("\ncharacter is:%s",a);
}

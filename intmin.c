#include<stdio.h> 
int main() 
{
   int a[30],i,n,max; 
   printf("Enter no of elements");
   scanf("%d", &n);  
   printf("Enter the elements")
   scanf("%d", &a[i]);
   }
   max=a[i]; 
   for(i=0;i<n;i++) 
   {
      if(a[i]>max)
      {
         max= a[i];
      }
   } 
   printf("max Element",max); 
   return 0;
  }
